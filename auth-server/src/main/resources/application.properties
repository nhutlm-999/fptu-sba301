spring.application.name=auth-server
# Port configuration
server.port=8081

# Config Auth Server
# Issuer
spring.security.oauth2.authorizationserver.issuer=http://auth-server.vn:8081

# Client 1 registration 'FeClient'
spring.security.oauth2.authorizationserver.client.feclient.registration.client-id=feclient

# Client secret should be encoded using a password encoder. For testing purposes, we can use {noop} to indicate that the secret is stored in plain text. In production, you should use a stronger encoding mechanism.
# Because Spring will call Passwoed encoder to compare the client secret, if you don't specify {noop}, it will throw an error "There is no PasswordEncoder mapped for the id "null""
#spring.security.oauth2.authorizationserver.client.feclient.registration.client-secret={noop}feclient-secret
spring.security.oauth2.authorizationserver.client.feclient.registration.client-secret=$2a$12$cuwt3XOpBINk6yu/9fRnNO5hWKp/Y01sEnBcCH40Ojo/MExY9ElAO
spring.security.oauth2.authorizationserver.client.feclient.registration.client-authentication-methods=client_secret_basic
spring.security.oauth2.authorizationserver.client.feclient.registration.authorization-grant-types=authorization_code, refresh_token
spring.security.oauth2.authorizationserver.client.feclient.registration.redirect-uris=http://localhost:8080/
spring.security.oauth2.authorizationserver.client.feclient.registration.scopes=openid,profile,email,artists.read

# Client X reg
#spring.security.oauth2.authorizationserver.client.mobileAppClient.registration.client-id=appclient`

spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=sba301_auth_server;encrypt=true;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=P@ssw0rd123
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# create -> tạo bảng theo thông tin từ entity
# create-drop -> tạo bảng theo thông tin từ entity, khi ứng dụng dừng sẽ xóa bảng
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.globally_quoted_identifiers=true